/*
 * This will setup some variables for use in the pixel
 * shader stage.
 */

/*
 * Entry point for the shader.
 */
void main()
{
    //gl_Position = gl_ModelViewProjectionMatrix * gl_Vertex;
	//gl_TexCoord[0] = gl_TextureMatrix[0] * gl_MultiTexCoord0;
	//gl_FrontColor = gl_Color;
}